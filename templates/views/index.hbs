<!DOCTYPE html>
<html lang="en">
<head>
    {{>head}}
    <title>{{title}}</title>
</head>
<body>
    <div class="loading">
        {{>loading}}
    </div>

    <section class="login">
        <div class="login__container">
            <h2>followthru</h2>
            <form class="login__form" method="POST" action="/friends">
                <input type="email" placeholder="Email" name="email">
                <input type="password" placeholder="Password" name="password">
                <button type="submit" class="login__login-btn">Login</button>
                <button class="login__apple-btn">Login with Apple</button>
                <button class="login__google-btn">Login with Google</button>
                <span>Don't have an account? <a href="/signup">Sign Up</a></span>
            </form>
    </section>
</body>
</html>
